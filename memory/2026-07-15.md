# 2026-07-14 — Super Appsheet: Gemini fix + LINE Integration

## Gemini API Key Bug (Critical)
- `GEMINI_API_KEY` in PM2 ecosystem had `\u2026` (ellipsis char) embedded at position 6 — corrupted key that looked valid
- Caused `'ascii' codec can't encode character '\u2026'` in `httpx._normalize_header_value` when google.genai SDK tried to send HTTP headers
- **Fix**: PM2 wasn't reading the key from `.env` correctly (had literal `…` in ecosystem.config.cjs)
- **Lesson**: Always verify env vars in PM2 ecosystem directly, not just `.env` file
- **Lesson**: If google.genai fails with ascii encoding error on header — suspect API key corruption first

## Voice API — POST /voice/process
- Endpoint in `voice_api.py` using Gemini to parse natural language → structured order
- Gemini system prompt defines Thai menu items (Pad Kra Pao Moo, etc.)
- JSON parser updated to handle nested `{}` properly and strip markdown code fences
- Works end-to-end: "ขอกะเพราหมู 2 จาน โต๊ะ 3" → `{action: identify, items: [Pad Kra Pao Moo x2], table_id: T03}`

## VoiceOrder.vue — Frontend
- Added table selection UI (dine-in grid + takeaway toggle)
- Cart bar shows table label
- Can change table before submitting
- Frontend proxy fixed: previously routed `/api/voice/*` to port 8115 (no service), now all `/api/*` → 8114

## LINE Integration (Built but NOT active yet)
- `src/line_service.py` — LINE bot webhook handler, Gemini parsing, message replies, table QR generation
- `src/line_api.py` — REST endpoints: `/line/webhook`, `/line/config`, `/line/push/:order_id`, `/line/qr/:table_id`
- `api.py` — LINE router registered
- **Awaiting credentials**: Needs `LINE_CHANNEL_SECRET`, `LINE_CHANNEL_ACCESS_TOKEN`, `LINE_BASIC_ID` from LINE Developers Console

## Services Status
- `super-appsheet-api` (8114) — ✅ Running, all endpoints working
- `super-appsheet-frontend` (54532) — ✅ Running
- `erp-core` (3000) — ✅ Running, MCP tools responding
- `tiktok-ugc-studio` — ✅ Running (do NOT touch — used by another team)

## Decisions
- **Google Sheets abandoned** — SQLite primary store for orders. Mock mode left in place but unused.
- **LINE Pay not implemented yet** — Phase 2 once LINE Messaging API is stable.
- **No ERP Core server.js modifications** — Direct SQLite for operations MCP doesn't support.
# 2026-07-15 — TUS Auto-Generate Fix + Full Service Recovery + scoutQuickClone async Bug

## Full Service Recovery (01:50–02:20 UTC)

### Problem
All PM2 processes were dead. No services running on ports 8105, 8110, 8111, 8117, 8120.

### Recovery Steps
1. **Fixed `ecosystem.config.js`** — changed `interpreter: 'python3'` → `interpreter: '/usr/bin/python3'`, `script: 'uvicorn'` → `script: '__dirname + "/main.py"'` (PM2 was using corrupted venv python3 as script → SyntaxError)
2. **Added `__main__` block to `main.py`** — `if __name__ == "__main__": import uvicorn; uvicorn.run(app, host="0.0.0.0", port=8105)` (was missing)
3. **Started all services manually + via ecosystem**

### Current Status
| Service | Port | Status |
|---------|------|--------|
| tiktok-ugc-studio | 8105 | ✅ |
| tus-frontend | 8120 | ✅ |
| image-gen | 8110 | ✅ |
| video-gen | 8111 | ✅ |
| prompt-builder | 8117 | ✅ |
| scheduler | 8130 | ✅ |
| drive-service | 8132 | ✅ |

## 🐛 scoutQuickClone() async Bug — THE REAL ROOT CAUSE (02:55–03:10 UTC)

### The Mystery
After deploying `autoGenerateAll()` fix + clearing caches + unregistering service worker, ALL JS functions still `undefined` in browser:
- `autoGenerateAll: undefined`
- `switchTab: undefined`
- `goContentStep: undefined`
- `generateScript: undefined`

HTML elements existed, file on disk had fixes, but browser couldn't execute ANY function.

### Root Cause
**`scoutQuickClone()` at ~line 4645: `function` → `async function`**

A single `await` in a non-async function aborted parsing of the ENTIRE shared script block, making all functions undefined.

### Fix
Added `async` keyword. `node --check` confirmed. Browser functions all restored.

### Lesson
- `node --check` catches async/await syntax errors — run on every deploy
- When ALL functions are `undefined`, suspect a parse error — not caching

## 🖼️ Image Gen 400 + nginx /storage/ Fix (03:10–03:30 UTC)
- Nano Banana requires inputImage — added scrape logic
- Added nginx `/storage/` route → proxy to image-gen (port 8110)
- Auto Generate All verified end-to-end: Script → Image → Navigate to Step 4 ✅

# 2026-07-15 — 🎉 TIKTOK PUBLISHED! First Video via AitoEarn API

## The Breakthrough (12:11–12:18 UTC)
- User provided new API key: `ai_wexpixpktm1RaXrFXtFkjnQIWTV9UX5or7RVKOyiOP8NJCT0`
- Called `GET /api/v2/channels/accounts?types[]=tiktok` → got **accountId**: `tiktok_-0002TMst9pSO4krFHlfvM8HTY7Jz999H32U` (Putterfreshshop, 117 followers)
- Uploaded video via AitoEarn asset system (sign → R2 upload → confirm) → asset URL
- **Critical bug fix**: `publishAt` is REQUIRED by AitoEarn API (not documented as required in OpenAPI spec, only MCP error revealed it)
- **Correct path**: `POST /api/v2/channels/publish/flows` (NOT `publish-flows`)
- Created publish flow → `flowId: 2569f243`, `taskId: 6a577a8825273d65837c5ee6`
- Called `publishChannelTaskNow` → **STATUS 2 = PUBLISHED** ✅
- `platformWorkId`: `v_pub_file~v2-1.7662727799552051216`

## Code Updated
- `connect/tiktok_poster.py`: Full AitoEarn REST API integration
  - Added `_upload_asset()` method (sign → R2 upload → confirm)
  - Fixed path to `/api/v2/channels/publish/flows`
  - Required `publishAt` field (defaults to now if not specified)
  - Proper response parsing with `flow_id` and `tasks`
- `ecosystem.config.js`: Added env vars `AITOEARN_URL`, `AITOEARN_API_KEY`, `TIKTOK_AITOEARN_ACCOUNT_ID`
- PM2 restarted with `--update-env`

## Key API Details (AitoEarn)
| Detail | Value |
|--------|-------|
| User | `user_Wd99abto` / `6a5768d025273d65837c03d1` |
| API Key | `ai_wexpixpktm1RaXrFXtFkjnQIWTV9UX5or7RVKOyiOP8NJCT0` |
| TikTok Account ID | `tiktok_-0002TMst9pSO4krFHlfvM8HTY7Jz999H32U` |
| TikTok Nickname | Putterfreshshop |
| REST Base | `https://aitoearn.ai` |
| MCP Endpoint | `https://aitoearn.ai/api/unified/mcp` |
| Auth Header | `X-Api-Key` |

## Publish Flow (3 steps)
1. `POST /api/assets/uploadSign` → R2 `uploadUrl`
2. `PUT <uploadUrl>` with video binary
3. `POST /api/assets/{id}/confirm`
4. `POST /api/v2/channels/publish/flows` with `{content, publishAt, items}`
5. MCP: `publishChannelTaskNow` to publish immediately

## Next Steps
- [ ] Commit and push code changes
- [ ] Test end-to-end: enqueue video → auto-publish via scheduler
- [ ] Add auto-publish-now after flow creation (optional)
- [ ] Monitor TikTok for video performance

---

# 2026-07-15 — 3-Phase AitoEarn Refactor (12:20–12:40 UTC)

## Phase 1: Centralized AitoEarnClient
- Created `connect/aitoearn_client.py` — single AitoEarnClient class
- Operations: `list_accounts()`, `get_account()`, `list_platforms()`, `upload_asset()`, `create_publish_flow()`, `publish_now()`, `get_campaigns()`, `get_earnings()`, `get_affiliate_link()`
- Singleton `client = AitoEarnClient()` for easy import
- Auto-caches connected accounts (5min TTL)
- Committed: `7cc67e1`

## Phase 2: Clean Enqueue Flow
- Updated `POST /publisher/enqueue` to:
  - Validate video path exists
  - Auto-resolve AitoEarn account by platform
  - Return account nickname + avatar in response
- Added auto-platform detection (`tiktok` as default)
- Committed: `ed18d02`

## Phase 3: End-to-End Wire Publisher → AitoEarn
- Updated `connect/tiktok_poster.py` to use `AitoEarnClient` (thin wrapper)
- Updated `connect/__init__.py` to export `aitoearn` + `poster`
- Full end-to-end test:
  - `POST /publisher/enqueue` → `pq_6b60a07c` created
  - `POST /publisher/pq_6b60a07c/post-now` → R2 upload → flow `89b7f55f` → Published ✅
- Committed: `0eea24b`

## New API Endpoints
| Method | Path | Description |
|--------|------|-------------|
| GET | `/aitoearn/status` | Connection + platform count |
| GET | `/aitoearn/platforms` | Grouped by platform type |

## nginx Missing Route for /aitoearn/
- User reported `/aitoearn/status` returns HTML from public URL
- Root cause: nginx `/api/tiktok/` block strips prefix and proxies to 8105, but `/aitoearn/` has no nginx route
- Fix: Need to add `location ^~ /aitoearn/` block in nginx conf (or add `/aitoearn/` inside the existing `/api/tiktok/` block since both go to port 8105)
- Alternative: Use `/api/tiktok/aitoearn/status` path which is already proxy'd


---

# 2026-07-15 — Frontend Connect TUS to AitoEarn (12:40–12:56 UTC)

## Problem Discovered
User reported TUS frontend at `https://openhands.m2igen.com/tiktok/` showed no connected platforms — all "Loading..." forever.

## Root Cause — Frontend Completely Disconnected
`PFM_API = window.location.origin + '/ugc'` — this prefix **doesn't exist**.
- Calls like `/ugc/accounts` hit Express server (port 8120) → SPA fallback → serves `index.html` as text
- Browser tries to JSON-parse HTML → fails silently → "Loading..." forever

## Fixes Applied
1. **`PFM_API`** → `window.location.origin + '/api/tiktok'` (nginx proxies `/api/tiktok/*` → 8105)
2. **`pfmFetchAccounts()`** — added `aeFetchStatus()` to fetch AitoEarn platforms + merge with local accounts
3. **`pfmPostNow()`** — rewired to use `POST /api/publisher/enqueue` → `POST /api/publisher/{id}/post-now`
4. **`pfmSchedule()`** — rewired to use enqueue with `schedule_at`
5. **`pfmFetchHistory()`** — rewired to use `GET /publisher/queue`
6. **`loadAccounts()`** — added AitoEarn accounts alongside local session accounts
7. **`loadScheduledPosts()`** — now calls `/publisher/queue` instead of `/posts/scheduled`
8. **Auto `post-now`** — `pfmPostNow()` auto-publishes immediately after enqueue and upload

## nginx Routes Added
```
location ^~ /api/aitoearn/ { proxy_pass http://127.0.0.1:8105; }
location ^~ /api/publisher/ { proxy_pass http://127.0.0.1:8105; }
```

## Verified Working
- `https://openhands.m2igen.com/api/aitoearn/platforms` → JSON with 6 platforms ✅
- `https://openhands.m2igen.com/api/publisher/queue` → JSON queue ✅
- TUS frontend → Connections tab shows 6 AitoEarn platforms ✅
- TUS frontend → Publisher → Post Wizard works end-to-end ✅
- Auto-scheduler posting: `pq_db17438c` auto-posted via scheduler ✅

## Architecture (Final)
```
Browser → https://openhands.m2igen.com/tiktok/ → nginx → Express:8120 (static files)
Browser JS → /api/tiktok/* → nginx strips prefix → backend:8105
Browser JS → /api/aitoearn/* → nginx strips prefix → backend:8105  
Browser JS → /api/publisher/* → nginx strips prefix → backend:8105
```

## Commit: abe69a4
"Frontend: Connect TUS to AitoEarn — fix PFM_API, wired accounts + publisher"
