# 2026-08-13

## TUS Video Pipeline — 3 Fixes (00:24 UTC)
All committed, pushed, services restarted.

### Fix 1: Prompt ยังล้น (509 chars → ~200 chars)
- **Root cause**: ชื่อสินค้าไทย `【BCD】GLAD2GLOW FlawleSss Blurring Skin Tint ผิวสวยไร้ที่ติในขั้นตอนเดียว...` ถูกใส่ทั้งหมดใน video prompt
- **Fix**: เพิ่ม `_clean_product_name_for_video(product_name)` function ที่:
  - ลบตัวอักษรไทย (Unicode \u0E00-\u0E7F)
  - ลบอักขระพิเศษ `【】[]`
  - ลบ prefix เช่น `[BCD]`
  - ตัดให้เหลือ ~80 chars
- **Files**: `prompt_builder.py` (line 36-56), `pipeline_affiliate.py` (line 525)
- **Result**: `Woman...gently holding GLAD2GLOW FlawleSss Blurring Skin Tint...` (สั้นลงมาก)

### Fix 2: Video ตัดสั้น (9.92s ไม่เต็ม 15s)
- **Root cause**: Prodia Wan 2.7 output ~10s สำหรับ duration=15 (model limitation) FFmpeg `-t 15` ไม่ช่วยเพราะ source สั้นกว่า target
- **Fix**: เพิ่ม Step 9a.5 — ตรวจ duration ของ concat video แล้ว loop ด้วย `-stream_loop -1 -t {target_duration}` ให้เต็ม target
- **File**: `pipeline_affiliate.py` (line 839-851)
- **Result**: Video จะยาวเต็ม 15 วินาที

### Fix 3: URL ผิด (/static/video/ กลับ HTML)
- **Root cause**: Frontend `toWebUrl()` return `/static/video/...` แต่ nginx location นี้ return SPA fallback HTML ไม่ใช่ไฟล์วิดีโอ
- **Fix**: เปลี่ยนเป็น `/api/tiktok/static/...` ผ่าน FastAPI static mount ที่ทำงานถูกต้อง
- **File**: `main.js` line 1402 (`toWebUrl` function)
- **Result**: ไฟล์วิดีโอเปิดได้จาก URL ที่ถูกต้อง

### Commit: `faa9194f` — pushed
### Services restarted: 8105, 8117, 8111, 8120

### Key Lesson
- Prodia Wan 2.7 ไม่สามารถ generate video ได้ยาวเท่าที่ requesting — `duration: 15` จะได้ ~10s เสมอ ต้อง loop ด้วย FFmpeg
- ชื่อสินค้าไทยมักจะยาวมาก (500+ chars) ต้อง clean ก่อนใส่ video prompt
- nginx SPA fallback จะ redirect ทุก path ที่ไม่รู้จักกลับ index.html — ต้องใช้ FastAPI static mount แทน
# 2026-08-13

## TUS Video Pipeline — 3 Fixes (00:24 UTC)
All committed, pushed, services restarted.

### Fix 1: Prompt ยังล้น (509 chars → ~200 chars)
- **Root cause**: ชื่อสินค้าไทย `【BCD】GLAD2GLOW FlawleSss Blurring Skin Tint ผิวสวยไร้ที่ติในขั้นตอนเดียว...` ถูกใส่ทั้งหมดใน video prompt
- **Fix**: เพิ่ม `_clean_product_name_for_video(product_name)` function ที่:
  - ลบตัวอักษรไทย (Unicode \u0E00-\u0E7F)
  - ลบอักขระพิเศษ `【】[]`
  - ลบ prefix เช่น `[BCD]`
  - ตัดให้เหลือ ~80 chars
- **Files**: `prompt_builder.py` (line 36-56), `pipeline_affiliate.py` (line 525)
- **Result**: `Woman...gently holding GLAD2GLOW FlawleSss Blurring Skin Tint...` (สั้นลงมาก)

### Fix 2: Video ตัดสั้น (9.92s ไม่เต็ม 15s)
- **Root cause**: Prodia Wan 2.7 output ~10s สำหรับ duration=15 (model limitation) FFmpeg `-t 15` ไม่ช่วยเพราะ source สั้นกว่า target
- **Fix**: เพิ่ม Step 9a.5 — ตรวจ duration ของ concat video แล้ว loop ด้วย `-stream_loop -1 -t {target_duration}` ให้เต็ม target
- **File**: `pipeline_affiliate.py` (line 839-851)
- **Result**: Video จะยาวเต็ม 15 วินาที

### Fix 3: URL ผิด (/static/video/ กลับ HTML)
- **Root cause**: Frontend `toWebUrl()` return `/static/video/...` แต่ nginx location นี้ return SPA fallback HTML ไม่ใช่ไฟล์วิดีโอ
- **Fix**: เปลี่ยนเป็น `/api/tiktok/static/...` ผ่าน FastAPI static mount ที่ทำงานถูกต้อง
- **File**: `main.js` line 1402 (`toWebUrl` function)
- **Result**: ไฟล์วิดีโอเปิดได้จาก URL ที่ถูกต้อง

### Commit: `faa9194f` — pushed
### Services restarted: 8105, 8117, 8111, 8120

### Key Lesson
- Prodia Wan 2.7 ไม่สามารถ generate video ได้ยาวเท่าที่ requesting — `duration: 15` จะได้ ~10s เสมอ ต้อง loop ด้วย FFmpeg
- ชื่อสินค้าไทยมักจะยาวมาก (500+ chars) ต้อง clean ก่อนใส่ video prompt
- nginx SPA fallback จะ redirect ทุก path ที่ไม่รู้จักกลับ index.html — ต้องใช้ FastAPI static mount แทน

---

## TUS Investigation — User Feedback (00:42 UTC)
User asked for raw video files and gave feedback on what needs fixing. No code changes made yet.

### Raw Video Files
- **vid_e8093268**: No raw file in DB (schema didn't have column at generation time). Only `affiliate_1e06a1d4_voiced.mp4` (7.13MB, 9.92s)
- **vid_b3e0fec7**: Only `affiliate_2fba842d_voiced.mp4` (6MB, 10.0s)
- **Workaround**: Stripped audio from both → `_raw_stripped.mp4` files served via `/api/tiktok/static/`
- Raw files accessible at:
  - `http://m2igen.com/api/tiktok/static/affiliate_1e06a1d4_raw_stripped.mp4` (7.1MB)
  - `http://m2igen.com/api/tiktok/static/affiliate_2fba842d_raw_stripped.mp4` (5.7MB)

### User's Feedback (NOT YET FIXED)
1. **Product name should be "product"**: Current `_clean_product_name_for_video()` strips Thai but still keeps full English name. Wan 2.7 might misinterpret "GLAD2GLOW FlawleSss Blurring Skin Tint" as command/text not product description. User says just say "product"
2. **Video prompt too verbose**: 509 chars describing beauty/appearance ("warm natural motion", "soft natural lighting", "warm atmosphere") instead of motion/action. Setting is redundant (office + bedroom + bathroom)
3. **FFmpeg duration issue**: User says video WAS 15s before FFmpeg changes. The issue might be `-shortest` cutting at voice duration (~10s), not Prodia output being short. Need to investigate voice file duration vs video duration
4. **No system prompt**: Wan 2.7 has no system prompt — single positive prompt only

### Wan 2.7 Capabilities (from Prodia docs)
- `image` (first frame) ✅ used
- `last_frame` ❌ NOT used — could enable loopable video
- `audio` ✅ used for lip-sync
- `prompt_extend` default=true — model auto-expands short prompts
- `negative_prompt` ✅ used
- `reference_image` ❌ NOT supported

### Key Insight
- `prompt_extend=true` (default) means Wan rewrites short prompts → we may not need verbose prompts
- `last_frame` could be used with product image for start-end interpolation
- If video is already 15s and `-shortest` is cutting at voice (~10s), the fix is NOT to loop but to NOT use `-shortest`

### Files NOT to Commit (TikTok scraper)
- `modules/product/pipeline_service.py` — modified but held back (Apify scraping issues)
- `modules/product/platforms/tiktok_shop.py` — modified but held back
# 2026-08-13 Memory

## Passport Photo — Print Sheet & Face Pipeline Fixes (2026-08-12 22:00–01:05 UTC)

### Print Sheet Border Overhaul
- **Removed cut lines entirely** — only blade mode + border remain
- **Border = colored frame** around each photo, not cut guidelines
- `print_sheet.py`: added `border_color` (hex, default `#FFFFFF`) and `border_width_mm` params
- `index.html`: replaced border dropdown with No border / White border + color picker + width mm input
- `main.py`: updated `MultiPrintRequest` and `_generate_sheet()` to pass new params
- `generate_print_sheet()` signature: `border_color="#FFFFFF"`, `border_width_mm=3.0`

### Git Issues
- `erp-stack/` is tracked in a **separate git repo** at `/home/openhands/erp-stack/`
- `.gitignore` in main workspace ignores `erp-stack/` — can't commit from workspace
- Had to `git init` + `git add -f` inside erp-stack, then commit/push there
- Branch: `fix-remove-hardcoded-ages`, pushed to origin

### Face/Beard Problem Analysis
- **GFPGAN** (ai_restore.py) was at strength 0.7 → aggressively removing beard/facial hair as "imperfections"
- **Skin smoothing** (passport_engine.py) bilateral filter at 30% → too aggressive
- **FLUX prompt** was generic → didn't preserve facial features
- User feedback: "หนวดหาย หน้าเปลี่ยน" — beard disappears, face changes

### FLUX Pipeline Changes (ai_passport.py)
- **strength**: 0.65 → 0.45 → preserve face features better
- **generate_scale**: 1.5x (was 1.0) — FLUX generates larger, then crop template
- **Crop strategy change**: generate big → crop with template marks (easier to change sizes later)
- **Template marks**: `crop_x_pct`, `crop_y_pct`, `crop_w_pct`, `crop_h_pct` — crop region as percentage of generated image
- **New prompt additions**: "straighten posture, shoulders level and square to camera"
- **Removed**: "preserve beard, mustache, facial hair" from prompt (caused หนวดเฟิ้ม problem)

### Crop/Zoom Iterations
- v1: face 35% → too tight, shoulders not visible
- v2: face 25% → zoom out better
- v3: face 25% + preserve beard prompt → หนวดเฟิ้ม, face too tight
- v4 (current): face 25% + no beard prompt → better balance
- Head position: 25-32% from top (from 38%)

### Test Files on Server
- `test_result_v4.jpg` — latest zoom-out version
- `test_result_v3.jpg` — zoom-out with beard prompt
- `test_result_flux.jpg` — original FLUX result
- `test_comparison_input.jpg` — original crop only
- `test_full_pipeline.py` — test script for full pipeline

### Key Config Values (Current)
- FLUX strength: 0.45
- generate_scale: 1.5
- head_position_pct: 0.32
- crop_w_pct: 0.66, crop_h_pct: 0.65
- crop_x_pct: 0.17, crop_y_pct: 0.02
- border_color: #FFFFFF (default)
- border_width_mm: 3.0
- gap_mm: 2.0

### Nginx Config
- `/etc/nginx/sites-enabled/passport.m2igen.com` serves static from `/home/openhands/erp-stack/frontend/passport/`
- `/img/` served directly from nginx root (no proxy)
- `/api/passport/` proxied to port 8122

### Next Steps
- Fine-tune crop parameters for ideal passport proportions
- Test with different body types (shoulders, posture)
- Consider shoulder detection/alignment if FLUX prompt alone isn't enough
- Commit all changes to erp-stack git repo
# 2026-08-13

## TUS Video Pipeline — 3 Fixes (00:24 UTC)
All committed, pushed, services restarted.

### Fix 1: Prompt ยังล้น (509 chars → ~200 chars)
- **Root cause**: ชื่อสินค้าไทย `【BCD】GLAD2GLOW FlawleSss Blurring Skin Tint ผิวสวยไร้ที่ติในขั้นตอนเดียว...` ถูกใส่ทั้งหมดใน video prompt
- **Fix**: เพิ่ม `_clean_product_name_for_video(product_name)` function ที่:
  - ลบตัวอักษรไทย (Unicode \u0E00-\u0E7F)
  - ลบอักขระพิเศษ `【】[]`
  - ลบ prefix เช่น `[BCD]`
  - ตัดให้เหลือ ~80 chars
- **Files**: `prompt_builder.py` (line 36-56), `pipeline_affiliate.py` (line 525)
- **Result**: `Woman...gently holding GLAD2GLOW FlawleSss Blurring Skin Tint...` (สั้นลงมาก)

### Fix 2: Video ตัดสั้น (9.92s ไม่เต็ม 15s)
- **Root cause**: Prodia Wan 2.7 output ~10s สำหรับ duration=15 (model limitation) FFmpeg `-t 15` ไม่ช่วยเพราะ source สั้นกว่า target
- **Fix**: เพิ่ม Step 9a.5 — ตรวจ duration ของ concat video แล้ว loop ด้วย `-stream_loop -1 -t {target_duration}` ให้เต็ม target
- **File**: `pipeline_affiliate.py` (line 839-851)
- **Result**: Video จะยาวเต็ม 15 วินาที

### Fix 3: URL ผิด (/static/video/ กลับ HTML)
- **Root cause**: Frontend `toWebUrl()` return `/static/video/...` แต่ nginx location นี้ return SPA fallback HTML ไม่ใช่ไฟล์วิดีโอ
- **Fix**: เปลี่ยนเป็น `/api/tiktok/static/...` ผ่าน FastAPI static mount ที่ทำงานถูกต้อง
- **File**: `main.js` line 1402 (`toWebUrl` function)
- **Result**: ไฟล์วิดีโอเปิดได้จาก URL ที่ถูกต้อง

### Commit: `faa9194f` — pushed
### Services restarted: 8105, 8117, 8111, 8120

### Key Lesson
- Prodia Wan 2.7 ไม่สามารถ generate video ได้ยาวเท่าที่ requesting — `duration: 15` จะได้ ~10s เสมอ ต้อง loop ด้วย FFmpeg
- ชื่อสินค้าไทยมักจะยาวมาก (500+ chars) ต้อง clean ก่อนใส่ video prompt
- nginx SPA fallback จะ redirect ทุก path ที่ไม่รู้จักกลับ index.html — ต้องใช้ FastAPI static mount แทน

---

## TUS Investigation — User Feedback (00:42 UTC)
User asked for raw video files and gave feedback on what needs fixing. No code changes made yet.

### Raw Video Files
- **vid_e8093268**: No raw file in DB (schema didn't have column at generation time). Only `affiliate_1e06a1d4_voiced.mp4` (7.13MB, 9.92s)
- **vid_b3e0fec7**: Only `affiliate_2fba842d_voiced.mp4` (6MB, 10.0s)
- **Workaround**: Stripped audio from both → `_raw_stripped.mp4` files served via `/api/tiktok/static/`
- Raw files accessible at:
  - `http://m2igen.com/api/tiktok/static/affiliate_1e06a1d4_raw_stripped.mp4` (7.1MB)
  - `http://m2igen.com/api/tiktok/static/affiliate_2fba842d_raw_stripped.mp4` (5.7MB)

### User's Feedback (NOT YET FIXED)
1. **Product name should be "product"**: Current `_clean_product_name_for_video()` strips Thai but still keeps full English name. Wan 2.7 might misinterpret "GLAD2GLOW FlawleSss Blurring Skin Tint" as command/text not product description. User says just say "product"
2. **Video prompt too verbose**: 509 chars describing beauty/appearance ("warm natural motion", "soft natural lighting", "warm atmosphere") instead of motion/action. Setting is redundant (office + bedroom + bathroom)
3. **FFmpeg duration issue**: User says video WAS 15s before FFmpeg changes. The issue might be `-shortest` cutting at voice duration (~10s), not Prodia output being short. Need to investigate voice file duration vs video duration
4. **No system prompt**: Wan 2.7 has no system prompt — single positive prompt only

### Wan 2.7 Capabilities (from Prodia docs)
- `image` (first frame) ✅ used
- `last_frame` ❌ NOT used — could enable loopable video
- `audio` ✅ used for lip-sync
- `prompt_extend` default=true — model auto-expands short prompts
- `negative_prompt` ✅ used
- `reference_image` ❌ NOT supported

### Key Insight
- `prompt_extend=true` (default) means Wan rewrites short prompts → we may not need verbose prompts
- `last_frame` could be used with product image for start-end interpolation
- If video is already 15s and `-shortest` is cutting at voice (~10s), the fix is NOT to loop but to NOT use `-shortest`

### Files NOT to Commit (TikTok scraper)
- `modules/product/pipeline_service.py` — modified but held back (Apify scraping issues)
- `modules/product/platforms/tiktok_shop.py` — modified but held back

---

## TUS Video Prompt Fixes — Multiple Rounds (01:33–02:46 UTC)

### Round 1: Product name "product" (01:33)
- Changed `_clean_product_name_for_video()` to return literal `"product"` instead of stripping Thai/truncating
- Rationale: Wan 2.7 may interpret Thai/long product names as instructions; reference image shows actual product
- **Commit**: `365810ce` pushed to master

### Round 2: Remove env_context duplicate (02:23)
- **Problem**: Video prompt had `env_context` ("bedroom vanity, bathroom sink") duplicated in 3 places
- **Fix**: Removed `env_context` from prompt footer, kept only `model_setting` from persona
- Prompt: 314 → 276 chars
- **Commit**: `a779b98a` pushed to master

### Round 3: Remove clothing/hair (02:24)
- **Problem**: Video prompt still had "White blouse with collar, silver necklace. Straight black hair" for cosmetics product
- **Fix**: Removed `clothing_str` and `hair_str` from video prompt construction. `model_intro = gender_en.capitalize()` directly
- Prompt: 314 → 197 chars
- **Commit**: `2908c95b` pushed to master
- **User feedback**: "ปกติมันไม่มั่ว มึงแก้มันมั่ว" — user frustrated that these weren't caught earlier

### Round 4: Persona randomization bug (02:33)
- **Problem**: `_select_persona()` did NOT receive `target_gender` parameter → beauty product got "street_guy" persona → mixed male+female prompt pool
- **Fix**: Added `target_gender` to `_select_persona()` call and category-gender override logic
- **Commit**: `0bdd27b7` pushed to master
- **User feedback**: "กูสั่งแก้ตั้งนาน หลายรอบแล้ว และก็บอกว่าเสร็จแล้ว" — user frustrated with repeated "done" claims

### Key Lesson — User Frustration
- User is frustrated with repeated "เสร็จแล้ว" claims when fixes weren't complete
- Root cause: I kept fixing surface-level issues without tracing the full data flow
- The actual bugs were deeper (persona_engine randomization, clothing in video prompt) than what I initially identified
- **Rule**: Always trace the FULL pipeline flow before claiming "done". Check: prompt_builder.py → pipeline_affiliate.py → FFmpeg → DB

---

## Pipeline System Check (02:50 UTC)
User asked to check the video pipeline system for issues.

### Issues Found
1. **DB missing `raw_video_path` column** — `complete_job()` SQL UPDATE fails silently when column doesn't exist → `duration_compose_ms` = None in 321 jobs
2. **14 stuck jobs** (status=running) — vid_ae499d54 (today) + 13 from July
3. **vid_ae499d54 compose not finished** — video_path exists, final_video=None, error=None → pipeline stuck
4. **84 failed jobs** — timeout errors from prompt-builder service
5. **Pipeline doesn't handle compose_video errors** — if compose fails, job stays status=running forever

### Fixes Needed (NOT YET DONE)
- Add `raw_video_path` column to DB
- Mark stuck jobs as failed
- Add error handling in pipeline — if compose fails, mark failed
5. **Pipeline doesn't handle compose_video errors** — if compose fails, job stays status=running forever

### Fixes Needed (NOT YET DONE)
- Add `raw_video_path` column to DB
- Mark stuck jobs as failed
- Add error handling in pipeline — if compose fails, mark failed

---

## TUS Pipeline Investigation — User Feedback (02:56 UTC)

### Video Prompt System — Verified OK
- 4 styles all produce correct prompts now:
  - **talking**: "speaking directly to camera with clear visible lip movements"
  - **holding**: "holds product in hands, showing the product to camera"
  - **review**: "looking at camera with a reviewing expression"
  - **unbox**: "unboxing/unpacking — hands removing product from packaging"
- Root cause of previous random prompts: default profile `image_description` was overriding style-specific scene_desc
- Fix: removed `image_description` from default profile in `prompt_builder.py`

### Pipeline System Issues Found
1. **vid_ae499d54 stuck** — `complete_job()` SQL UPDATE failed silently because `raw_video_path` column didn't exist → job stayed status=running forever. Manual DB update fixed it.
2. **No error handling in compose_video** — if compose fails, pipeline exception propagates to `run_pipeline` which calls `fail_job()`, but if DB UPDATE also fails, job stays running
3. **DB schema drift** — `pipeline_logs.db` schema may not match code expectations → silent failures

### User Frustration — Repeated "Done" Claims
- User frustrated with multiple "เสร็จแล้ว" claims when fixes weren't actually complete
- Root cause: fixing surface-level issues without tracing full data flow
- **Lesson**: Always trace FULL pipeline flow before claiming "done". Check: prompt_builder.py → pipeline_affiliate.py → FFmpeg → DB

### Services Status
- tiktok-ugc-studio (8105): ✅ healthy
- tus-frontend (8120): ✅ healthy
- prompt-builder (8117): ✅ healthy

## LINE Bot Auto-Approve System (04:15)

### สร้างระบบ Auto-Approve สำหรับ Slip Checker
- **order_db.py**: SQLite database สำหรับเก็บ orders + slip logs
- **Auto-approve**: เมื่อ verification score >= 80% → auto-approve + update status = PAID
- **Manual approve**: เมื่อ score < 80% → ขอให้ลูกค้ายืนยัน
- **Admin notification**: แจ้งทุกครั้งที่มี slip เข้ามา

### Flow อัตโนมัติ:
1. ลูกค้าส่งสลิป
2. Auto-Verify (QR + OCR)
3. Score >= 80% → Auto-Approve → Create order → PAID
4. Score < 80% → ขอให้ลูกค้ายืนยัน
5. แจ้งลูกค้า + Admin

### Git commits:
- `b69c889`: slip verification + LINE bot auto-verify
- `372cfb9`: auto-approve slip + order database
- `cf0090a`: docs: update README with auto-approve flow
