# 2026-07-18 — TikTok UGC Studio E2E + Prodia Wan 2.7 Fix

## Prodia Wan 2.7 Duration Fix
- Prodia API **removed** `duration`/`resolution`/`negative_prompt` from config schema (schema validation)
- But `duration: 8` + `resolution: "720P"` still works when inside config — 7.979s ✅
- `duration: 16` triggers rejection — max is **15** per docs, but actual output is ~10s
- `txt2img.v1` endpoint is sync-only (async 404s) — used `img2vid.v1` instead
- **Pricing**: Flat $0.03 per Wan 2.7 job (same for 5s or 15s)

## Pipeline Performance
- Full pipeline run (TUS recipe, 1 scene): **157s**, **$0.069**
- Video gen step: 9MB output, ~10.3s actual duration at 720P
- All models used: Gemini TTS (Aoede) → Prodia Nano Banana (image) → Prodia Wan 2.7 (video) → FFmpeg compose

## Commits Today (3)
1. `ea74254` — AitoEarn sync + Prodia token + import fix
2. `54d86f6` — image resize 4800→2048 + PIL import
3. `ddea01d` — Prodia Wan 2.7: remove duration/resolution from config
4. `ab0036d` — TUS recipe duration 8→15 (Prodia max)

## Bugs Found
1. `pipeline_affiliate.py` L526 hardcoded `job_type="inference.wan2-7.img2vid.v1"` — overrides prodia_client default
2. Prodia async API rejects `negative_prompt` for Wan 2.7
3. Pipeline generates only 1 scene (hardcoded `# 1 scene only`)

## Still Pending
- Multi-scene video generation (6 scenes → 6 videos → concat)
- 60 products with no images (waiting for Antigravity)
- Product scraper (no Playwright browser binaries)
- Frontend video preview URL
