# 2026-07-16

## Phase 1: Data Pipeline — Title, Description, Hashtags (01:03-01:15 UTC)
- Added `title TEXT` and `description TEXT` columns to `post_queue` table (PRAGMA add-column migration)
- Updated `_pipeline_results` storage to include `title`, `description`, `hashtags`
- `/video/completed` now enriches from `pipeline_logs.db`: `hashtags`, `script` (truncated to 2200 for description)
- `post_queue.enqueue()` accepts `title` + `description` params, stores in INSERT
- `scheduler.enqueue_completed_video()` accepts same new params
- `publisher_enqueue` route passes title/description/hashtags through
- Frontend: `pubPostNow()` and `pubSchedule()` pass `title`, `description`, `hashtags` to enqueue
- Frontend: video cards show title (when available), hashtags row below cost
- JS validated: `node --check` passes
- PM2 restarted with `--update-env`
- ✅ Verified: `GET /video/completed` returns `title`, `description`, `hashtags` per video
- **Commit `e6a630a`** — feat: Phase 1 — title, description, hashtags in pipeline → publisher data flow

## Phase 2: Multi-Platform Support (01:15-01:29 UTC)
- **`aitoearn_client.py`**: `publish_video()` now accepts `platform` param (default "tiktok"), auto-resolves account via `get_account_for_platform()`, passes platform through to `create_publish_flow()`
- **`tiktok_poster.py`**: Renamed class to `SocialPoster` (kept `TikTokPoster` as backward-compat alias), `post()` accepts `platform` + `title` + `description`, passes them to `publish_video()`
- **`connect/__init__.py`**: Exports `SocialPoster`, `TikTokPoster`, `poster`
- **`scheduler.py`**: `enqueue_completed_video()` accepts `platform` + `account_id` + `title` + `description`
- **`main.py`**: `post-now` and `retry` routes pass `platform`, `title`, `description` from post record to poster
- **Frontend**: Added platform `<select>` dropdown to every video card, `updateCardPlatform()` helper, `pubPostNow()`/`pubSchedule()` read platform from dropdown
- **JS validated**: `node --check` passes
- ✅ Verified: 6 platforms detected via AitoEarn (TikTok, YouTube, Twitter/X, Pinterest, Facebook, Instagram)
- ✅ Verified: Instagram account auto-resolves correctly (`instagram_17841401883010014` → ezyshoponline)
- **Commit `03dbd65`** — feat: Phase 2 — multi-platform support across poster, scheduler, frontend
# 2026-07-16

## Phase 1: Data Pipeline — Title, Description, Hashtags (01:03-01:15 UTC)
- Added `title TEXT` and `description TEXT` columns to `post_queue` table (PRAGMA add-column migration)
- Updated `_pipeline_results` storage to include `title`, `description`, `hashtags`
- `/video/completed` now enriches from `pipeline_logs.db`: `hashtags`, `script` (truncated to 2200 for description)
- `post_queue.enqueue()` accepts `title` + `description` params, stores in INSERT
- `scheduler.enqueue_completed_video()` accepts same new params
- `publisher_enqueue` route passes title/description/hashtags through
- Frontend: `pubPostNow()` and `pubSchedule()` pass `title`, `description`, `hashtags` to enqueue
- Frontend: video cards show title (when available), hashtags row below cost
- JS validated: `node --check` passes
- PM2 restarted with `--update-env`
- ✅ Verified: `GET /video/completed` returns `title`, `description`, `hashtags` per video
- **Commit `e6a630a`** — feat: Phase 1 — title, description, hashtags in pipeline → publisher data flow

## Phase 2: Multi-Platform Support (01:15-01:29 UTC)
- **`aitoearn_client.py`**: `publish_video()` now accepts `platform` param (default "tiktok"), auto-resolves account via `get_account_for_platform()`, passes platform through to `create_publish_flow()`
- **`tiktok_poster.py`**: Renamed class to `SocialPoster` (kept `TikTokPoster` as backward-compat alias), `post()` accepts `platform` + `title` + `description`, passes them to `publish_video()`
- **`connect/__init__.py`**: Exports `SocialPoster`, `TikTokPoster`, `poster`
- **`scheduler.py`**: `enqueue_completed_video()` accepts `platform` + `account_id` + `title` + `description`
- **`main.py`**: `post-now` and `retry` routes pass `platform`, `title`, `description` from post record to poster
- **Frontend**: Added platform `<select>` dropdown to every video card, `updateCardPlatform()` helper, `pubPostNow()`/`pubSchedule()` read platform from dropdown
- **JS validated**: `node --check` passes
- ✅ Verified: 6 platforms detected via AitoEarn (TikTok, YouTube, Twitter/X, Pinterest, Facebook, Instagram)
- ✅ Verified: Instagram account auto-resolves correctly (`instagram_17841401883010014` → ezyshoponline)
- **Commit `03dbd65`** — feat: Phase 2 — multi-platform support across poster, scheduler, frontend

## Phase 3: Calendar & Batch Scheduling (01:29-01:45 UTC)
- **`scheduler.py`**: `bulk_schedule()` — bulk schedules videos across date range, 3 modes (random/fixed/sequential), videos cycle through all slots
- **`main.py`**: `POST /publisher/bulk-schedule` route
- **Frontend**: Batch Schedule button → modal with platform selector, date range, posts/day, time window, timing mode radio, live preview (shows schedule per day), "Schedule All" button
- **JS validated**: ✅ 0 errors (fixed multi-line string issues in confirm/alert)
- ✅ Verified: 2 test posts scheduled with random times in 08:00–12:00 window (09:32 + 09:58)
- **Commit `500b2fc`** — feat: Phase 3 — batch calendar scheduling

## Publisher UI Redesign & Bug Fixes (01:55-03:20 UTC)
### Audit — 7 issues found:
1. **Batch Schedule button broken** — Modal HTML not in DOM → inserted batch-schedule-modal
2. **Video preview modal can't close** — `classList.remove('open')` overridden by inline `display:flex` → changed to `style.display='none'`/`flex`
3. **Platform selector single-only** — Changed batch modal to checkbox multi-select
4. **Queue table breaks on mobile** — Replaced `<table>` with card-based layout
5. **Batch missing features** — Added multi-platform checkboxes + per-platform count preview
6. **Schedule button on cards** — Confirmed Post Now + Schedule visible on every card
7. **`pubSchedule()` used `prompt()`** — Replaced with proper dialog (date/time/platform/mode)
- **Commit `2e9214b`** — fix: publisher UI — modal close, batch modal, queue cards, schedule dialog

### Follow-up fixes:
- **Video audio persist after modal close**: `style.display='none'` didn't pause — fixed by adding `video.pause()` and `video.remove()` before hiding
- **Card redesign — remove dropdown, new flow**:
  - Removed platform `<select>` from thumbnail cards
  - **Post Now** → modal with multi-platform checkboxes → Post
  - **Schedule** → modal with multi-platform checkboxes + date/time/mode → Schedule
- **Commit `9323363`** — refactor: publisher cards — modal-based multi-platform post/schedule

### Bug fix — `html is not defined` + design polish:
- Added missing `var html = ''` before `for` loop in `loadPublisherQueue()`
- Removed thick card borders — flat layout: header + content, no `.card` wrapper
- Modern status badges: platform pill badges, colored status circles (✅⏳❌⚠)
- **Commit `dbe5d3c`** — fix: publisher queue html + redesign borders

## Current State
- **Repo**: `master`, latest commit `dbe5d3c`
- **Service running**: PM2 `tiktok-ugc-studio` on port 8105
- **Frontend**: `https://m2igen.com/tiktok/` → Publisher tab
- **AitoEarn API**: Key `ai_wexpixpktm1RaXrFXtFkjnQIWTV9UX5or7RVKOyiOP8NJCT0`
  - 6 platforms connected: TikTok (Putterfreshshop), Instagram (ezyshoponline), YouTube (Putter Gaming), Twitter/X, Pinterest, Facebook (M2I Gen)
  - `uploadSign` still broken (returns empty data) — public URL fallback in place
  - `publish/flows` may still return 400 — need MCP approach investigation

## Pending Items
- [ ] Fix AitoEarn `publish/flows` 400 — try MCP approach
- [ ] Test multi-platform post (Instagram/Facebook/YouTube) via new card modal flow
- [ ] Platform-specific content formatting adapters (title/description limits differ)
