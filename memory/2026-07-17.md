# Memory 2026-07-17

## Session: Product Scraped → Analysis → TUS Flow

### Context
- User has 60 Gemini-scraped products in PostgreSQL `productdb.products` (port 5435).
- The existing `GET /products/scraped` endpoint reads directly from PostgreSQL with asyncpg.
- The existing `POST /products/analyze-scraped` endpoint (in `main.py`) reads from PostgreSQL → enriches with Gemini keyword extraction → writes to `tus_products.db`.
- Flow goal: Product Scraped (60) → Analysis (normalize + Gemini enrich) → TUS Pipeline (video gen + post).

### Key Discovery
- `productdb` database is on **port 5435** PostgreSQL, NOT port 5432 (which has `erp_stack`).
- The `POST /products/analyze-scraped` endpoint already exists in main.py (done in a prior session before compaction).
- Need to verify it on the live server at m2igen.com.

### What was implemented
1. **Backend**: `GET /products/scraped` (asyncpg → PostgreSQL) in main.py
2. **Frontend**: "📦 Scraped Products (Gemini)" section in index.html under Products page
3. **Analysis**: `POST /products/analyze-scraped` — reads 60 products, extracts keywords via Gemini, imports into `tus_products.db`
4. **Auto Pipeline**: 3 modes in Video Wizard Step 4 — Manual, Auto Queue, Full Auto
# 2026-07-17 — Product Analysis System Expansion

## Session Context
- Friday, 09:40 UTC (expected auto-compaction/flush)
- Channel: webchat (direct)

## What Happened
- User asked about building out the Product Analysis system further:
  1. ✅ **Limit control** — Added `limit` parameter to `GET /api/v1/analyze/export?limit=N` (clamped 1-500)
  2. ✅ **Keyword search** — Added `keyword` parameter to search analyzed products across keywords[], title, title_th, description using PostgreSQL JSONB contains
  3. ✅ **Product Options (variants)** — Added `variants` JSON column to `AnalyzedProduct` model + ALTER TABLE migration
  4. ✅ **Auto Watch System** — Full CRUD API:
     - `POST /api/v1/watch` — create watch (keywords, source, limit, schedule)
     - `GET /api/v1/watch` — list watches
     - `POST /api/v1/watch/{id}/run` — run watch now
     - `PUT /api/v1/watch/{id}` — update watch
     - `DELETE /api/v1/watch/{id}` — delete watch
     - Scheduler updated to auto-run daily watches
  5. 💬 **LINE Integration (in progress)** — Adding product analysis handlers to existing LINE OA (@petepos)
  6. Explained OpenClaw vs direct LLM API integration (they're different paradigms)

## Files Modified
- `erp-stack/modules/product/analyzer_db.py` — Added keyword search (imports JSONB, or_, cast, String; modified get_analyzed_products)
- `erp-stack/modules/product/analyze_pipeline.py` — Updated wrapper to pass keyword/limit/offset
- `erp-stack/modules/product/main.py` — Added keyword/limit/offset to /api/v1/analyze/export endpoint + Auto Watch CRUD endpoints
- `erp-stack/modules/product/scheduler.py` — Added auto-run for keyword watches
- `erp-stack/modules/product/db_models.py` — Added variants column + ProductWatch model
- `super-appsheet/src/product_line_handler.py` — NEW: Product analysis LINE handlers (search, trending, stats, watch)
- `super-appsheet/src/line_service.py` — (in progress) Adding routing for product vs food queries

## Key Decisions
- **LINE for product analysis**: Use existing LINE OA (@petepos) for now, add intent routing to separate product vs food queries
- **Auto Watch**: Simple scheduler-based system using keyword search on analyzed_products, not full platform search
- **New LINE OA**: Planned but blocked — user couldn't get Messaging API working on new account
- **Product Options**: Column added but scrapers not yet populating it — needs platform scraper updates

## Open Questions
- User couldn't create new LINE OA with Messaging API — may need assistance
- Product Options scraping needs platform scraper modifications (tiktok_shop.py, shopee.py, lazada.py)
# 2026-07-17 — UGC Pipeline Overhaul 🔥 (11:09-12:09 UTC)

## Session Context
- Friday, 12:09 UTC (auto-compaction flush)
- Major overhaul of prompt-builder-service + script_gen.py

## What Was Done

### Gemini Consultation
- Asked Gemini 2.5-flash for strategic advice on UGC diversity
- Priority: Migrate Mistral→Gemini, Add Persona Injection, Add 6 new styles, New recipes

### Commit 1: Mistral→Gemini Migration + Persona + 6 New Styles (`0ef0415`)
- `prompt_builder.py`: Replaced all `_call_mistral_text()`/`_call_mistral_vision()` with `_call_gemini()`/`_call_gemini_vision()`
- Added `analyze_product_image()` using Gemini multimodal (base64 inline)
- Persona injection: 6 persona templates + `_select_persona()` + `_apply_persona_to_profile()`
- 6 new UGC styles: POV_Lifehack, ASMR_Texture, Split_Comparison, Street_Interview, Greenscreen_React, Aesthetic_Vlog
- 6 style folders created with system.prompt, master.prompt, user.template.prompt, negative.prompt
- 3 new recipes: tus_pas_8s.json, tus_secret_8s.json, tus_comparison_8s.json

### Commit 2: Microservice Split (`b83562a`)
- Split `prompt_builder.py` (1013→597 lines) into 4 modules:
  - `prompt_templates.py` — Maps, constants, template loaders (201 lines)
  - `gemini_client.py` — Gemini API calls + vision (212 lines)
  - `persona_engine.py` — Persona selection + injection (88 lines)
  - `prompt_builder.py` — Thin orchestrator (597 lines)

### Commit 3: Persona Sync with script_gen.py (`5ea9b36`)
- Added `speech_style`, `pacing`, `forbidden_phrases` to all 6 persona templates
- `build_script_system_prompt(persona)` — injects tone/voice/pacing into Gemini system prompt
- `generate_tiktok_review_script()` now accepts `persona` parameter (auto-generates if None)
- 13 STRICT output format rules (ห้าม emoji, "สวัสดี", กดติดตาม, ตัวเลข, ฯลฯ)
- Tested: Calm Professional → script ธรรมชาติ ไม่มีคำน่ารำคาญ ✅

### In Progress
- 2 new 15s recipe JSONs created (tus_pas_15s.json, tus_comparison_15s.json)
- `script_gen.py`: Added `adjust_prompt_for_duration()` for 15s timing constraints (45-55 words)
- `build_script_system_prompt()` now accepts `duration` param and calls `adjust_prompt_for_duration()`
- `generate_tiktok_review_script()` handles 15s duration with timing layer
- `SCRIPT_TEMPLATES` added 15s entry for fallback
- Need to: commit, restart prompt-builder-service, test
- Total: 10 styles, 9 recipes, 2 services (prompt-builder + script_gen)

## Architecture Decision
- **Pipeline**: Persona decided once at orchestrator level → passed to both prompt_builder AND script_gen
- **Persona scope**: Vibe, scene, lighting (for image/video) + speech_style, pacing, forbidden_phrases (for voice)
- **Gemini is sole LLM** — Mistral removed from prompt-builder, Fal.ai pending removal from video gen

## Commits
- `0ef0415` — Mistral→Gemini migration + persona injection + 6 new styles + 3 recipes
- `b83562a` — Microservice split (4 modules), UGC prompt files created
- `5ea9b36` — Persona sync with script_gen.py, enhanced persona templates

# 2026-07-17 — Router Agent + Git Push + LINE OA Setup (12:09-13:30 UTC)

## What Was Done

### Commit 4: 15s Recipes + Timing Constraints (`d60e7ab`)
- Created `tus_pas_15s.json`, `tus_comparison_15s.json`
- `script_gen.py`: Added `adjust_prompt_for_duration()` (45-55 Thai words, 3-4 segments)
- `build_script_system_prompt(persona, duration)` — duration param
- 3 files (+192/-5 lines)

### Commit 5: Router Agent Architecture (`7210ce2`)
- **Replaced hardcoded RECIPE_RULES** with `router_agent.py` — Gemini Router
- Created 3 core schema files in `schemas/`:
  - `pas_schema.json` — Problem → Agitate → Solve (flexible timing_ratio)
  - `comparison_schema.json` — Us vs Them (flexible timing_ratio)
  - `secret_hook_schema.json` — Secret/Gatekeeping hook (flexible timing_ratio)
- **recipe_system.py** simplified — loads schemas + calculates scene durations from timing_ratio × total_seconds
- **Deleted old recipe files**: tus.json, etsy.json, tus_8s.json, tus_15s.json, tus_pas_8s.json, tus_pas_15s.json, tus_secret_8s.json, tus_comparison_8s.json, tus_comparison_15s.json
- **Cleaned `prompt_templates.py`**: Removed `PRODUCT_CATEGORY_MAP` (20 rows), `LIGHTING_MAP` (7 cats), `_match_category()`, `_get_lighting()`
- **Cleaned `prompt_builder.py`**: Removed duplicate function defs, updated imports, integrated Router Agent into `analyze_product()`
- Router Agent call flow: `router_decide()` → structured JSON (recipe_type, duration, visual_style, persona, reason, scenes with calculated durations) → orchestrator distributes to Script Gen + Prompt Builder

### Git Push
- Generated new GitHub PAT, set remote URL with token
- Pushed 6 commits (`73a4020`..`7210ce2`) to `peesaderd/erp-stack.git`

### LINE OA Setup (@440kwftx)
- New LINE OA ID: `@440kwftx` (instead of @petepos)
- Channel ID: 2010746075
- Channel Secret: `2b12607da37b7c82131e9d16be1917f0`
- **Nginx fixed**: Changed `/line/` proxy from `http://127.0.0.1:18789` → `http://127.0.0.1:8114`
- **.env updated**: `LINE_CHANNEL_SECRET`, `LINE_BASIC_ID=@440kwftx`
- **super-appsheet-api restarted** with new credentials
- Webhook endpoint `https://m2igen.com/line/webhook` returns 200 (responds with error for unauthenticated test payloads but ready for real LINE traffic)

### Blocked / Pending
- ❌ **LINE Channel Access Token** — user says LINE OA Manager UI no longer has "Issue Channel Access Token" button (new UI changed). Screenshot can't be read by model (no vision support). Using old token `ziMu03o...lFU=` from @shopabc config. Need user to check/send the correct token.

## Architecture: Final State
```
router_agent.py (Router Agent) → structured JSON config (recipe, style, duration, persona)
    ↓
prompt_builder.py (Orchestrator)
    ├── prompt_templates.py (10 UGC styles, loaders)
    ├── gemini_client.py (Gemini API + vision)
    ├── persona_engine.py (6 personas, injection)
    ├── recipes/ (schema-based via recipe_system.py)
    └── schemas/ (3 core: pas, comparison, secret_hook)
```
