
# 2026-07-21 Memory

[existing content preserved - appending below]

---

## Session ~00:07–00:38 UTC (perpetuated from prior) — Aider DAMAGE #2 + model_casts fix + sort fix

### 🚨 Aider Strikes Again (Commit 1a8f30a → 45c5708 → edecd10)
- Aider auto-triggered and replaced the ENTIRE `index.html` (5899 lines → 285 lines) — deleted 5,858 lines
- Committed partial fixes on top that didn't help
- **Fix**: `git reset --hard 7a30752` (our clean commit) — reverted the damage
- **Lesson**: Aider keeps getting triggered somehow. The `opencode` + aider integration in the workspace keeps running. Need to disable or remove aider entirely.

### Model Casts Thai Feature Fix
- User reported model looks "แขก" (Indian/Middle Eastern), not Thai
- Updated `model_casts.json` with specific Thai descriptors:
  - Added: `monolid eyes`, `tan honey skin`, `high cheekbones`, `Southeast Asian facial features`
  - Added: `ethnic Thai woman` prefix instead of just `Thai woman`
  - Changed holding poses: `holding beside cheek`, `press against cheek`, `both hands presenting`
  - Added camera angles & background: `mirror selfie`, `close-up beauty shot`, `warm home atmosphere`
- Fixed Cyrillic text accidentally left in sporty_fresh appearance

### Publisher Sort Fix
- `/video/completed` had NO explicit sorting — jobs appeared in arbitrary order
- When multiple jobs had the same date (from filesystem scan), old jobs appeared on top
- **Fix**: Added `jobs.sort(key=lambda j: (created, job_id), reverse=True)` — newest first, job_id tiebreaker
- Changed filesystem `mtime` from `strftime("%Y-%m-%d")` (date only) to `.isoformat()` (full timestamp)
- Added `created` field to in-memory results (was missing)

### Topnav Cleanup
- Removed green connection status dot (`status-dot.online`) from topbar-actions
- Removed refresh button (`⟳`) from topbar-actions
- Both removed per user request: "มันกินพื้นที่"

### Hamburger Alignment
- Added `margin-left: -10px` to `.mobile-menu-btn` to align hamburger with content text below

### Commit
`6fb2a88` — "fix: publisher sort by created DESC + job_id tiebreaker, remove topnav dot/refresh, hamburger alignment"
