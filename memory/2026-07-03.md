# 2026-07-03 — Colab Pipeline Setup + Prompt Studio Consolidation

## Timeline
- **08:29 UTC** — User asked for prompt system status check
- **08:50 UTC** — Prompt Studio found deactivated; reactivated on port 8108
- **09:45-10:15 UTC** — **Point 1:** Synced TUS prompts (prompts/ + UGC_prompts/) → Prompt Studio as single source of truth
  - Sync'd 20+ prompt files across tiktok/, ugc/ (Holding_Product, Product_Usage, UGC_Review), and image/
- **10:15-11:00 UTC** — Rewrote script_gen.py to fetch from Prompt Studio API first, fallback to local disk
- **11:00-12:00 UTC** — Added MCP bridge: ERP Modular now proxies prompts/resources to Prompt Studio
  - Added `/prompts`, `/mcp/prompts`, `/mcp/resources` routes in erp-modular/main.py
  - Updated `mcp/erp_mcp_server.py` with prompt-related tools
- **12:00-12:30 UTC** — **Point 2:** Set up Colab Pipeline
  - Antigravity (quota exhausted) had built colab_pipeline.py + TUS_Colab.ipynb but never fully connected
  - Fixed `POST /colab/submit` endpoint: accepts `FullPipelineRequest` (product info, script, TTS)
  - Creates job with config.json + product image + TTS audio → `storage/colab_drive/_ready/{job_id}/`
  - Added Drive API integration but blocked by Google policy (service account can't upload to individual My Drive — needs Shared Drive)
  - **Solution:** Changed to **Colab Pull** mode — Colab curl `GET /colab/jobs/pending` + `GET /colab/jobs/{id}/download`
  - Added `POST /colab/jobs/{id}/complete` (receives base64 video) + `POST /colab/jobs/{id}/failed`
- **12:30-15:30 UTC** — User testing Colab notebook in browser
  - Wan 2.2 Diffusers model: `Wan2.2-I2V-A14B-Diffusers` (~12GB download, needs GPU 16GB+ VRAM)
  - Cell 4 flow: get_pending_jobs() → download_job() → run_wan_i2v() → compose_video() → mark_complete()
  - Multiple test jobs submitted but Colab kept disconnecting/runtime resets
  - Final job `16a50744` still pending: "La Glace Vita-C Brightening Serum 30ml" with real image + TTS
- **15:36 UTC** — Session ending. Colab still needs: Cell 1→2→3→4 run again after reconnect

## Key Decisions
- **Prompt Studio = source of truth** for all prompts. TUS script_gen.py calls Prompt Studio API first, falls back to local disk
- **Colab Pull Mode** (not Drive bridge) — Service account can't upload to individual Google Drive. Colab downloads jobs directly from TUS server
- Wan 2.2 Diffusers model is big (~12-18GB). First load slow; cached in Colab session temp memory (lost on disconnect)
- Pipeline state machine, auto-post, SAM3 quality gate, preset library all deferred for later

## Technical Notes
- TUS server IP: `89.167.82.205:8105`
- TUS port: 8105 (was 8106, needs check)
- Prompt Studio: port 8108
- Drive Service: port 8132 (service account: super-appsheet@peteai-494609.iam.gserviceaccount.com)
- folder structure created on Drive: TUS_Pipeline/{_ready,_done,_archive,libs}/ (but no write access for upload)
## Infrastructure Status (session end)
- **ERP Modular** (port 8102) → running
- **TUS** (port 8105) → running
- **Prompt Studio** (port 8108) → running
- **Drive Service** (port 8132) → running
- **Colab** → disconnected; needs fresh run of cells 1→2→3→4
