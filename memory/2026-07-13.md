structuredContent:
{
  "content": "\n## Part 16 — Super AppSheet POS: 39 menu images via KREA v2 Medium (2026-07-13 16:25)\n\n### What Was Done\n- **FAL_KEY saved permanently** to 5 .env files (erp-stack, image-gen, super-appsheet, openclaw .env, .openclaw/.env)\n- **Deleted all 39 old images** (Pollinations.ai + older KREA batches)\n- **Generated ALL 39 menu images** via KREA v2 Medium (`fal-ai/krea/v2/medium/text-to-image`)\n- **Images verified** HTTP 200 on all 39, 43 MB total, size ~900-1400 KB each\n- **Cache headers fixed**: Express server `maxAge: 0` + `Cache-Control: no-store, no-cache, must-revalidate`\n- **Cache-busting added**: `?v=2` query param in `PublicMenu.vue` image URLs\n- **Frontend rebuilt** + both PM2 services restarted (super-appsheet-api, super-appsheet-frontend)\n\n### Image Generation Scripts Created\n| Script | Path |\n|--------|------|\n| `gen_all_krea.py` | `/home/openhands/super-appsheet/scripts/gen_all_krea.py` |\n| `gen_krea.py` | `/home/openhands/super-appsheet/scripts/gen_krea.py` |\n\n### Architecture (current)\n| Port | Service |\n|:----:|:--------|\n| 54531 | nginx reverse proxy → Express on 54532 (user-facing URL) |\n| 54532 | Express server (Vue SPA, static files, menu images) |\n| 8114 | Python API (pos_api.py, pos_engine.py) |\n| 8115 | voice-pos backend |\n\n### Key Facts\n- **nginx proxies port 54531 → Express on 54532** — user's URL `http://89.167.82.205:54531/menu?table=T11` works via this chain\n- **Browser cache** was the root cause of \"ยังเห็นเป็นรูปเก่า\" — fixed with `no-store` headers + `?v=2`\n- **KREA v2 Medium** cost ~$0.01/image on fal.ai\n- **39 menu items total**: 8 appetizers, 12 main courses, 6 desserts, 9 beverages, 4 side dishes\n\n### Critical Rules Added\n- **DO NOT change ports** without asking — user was using port 54531 for frontend, not 54532\n- **When the user swears at you, LISTEN** — their frustration is real and I caused it by moving things unnecessarily\n- **Test on user's actual URL** before saying it's fixed\n- **nginx configuration** must be checked when HTTP headers differ between ports\n\n## Part 17 — 🔴 Pipeline P0: `ratio` bug on git HEAD (2026-07-13 16:35)\n\n### What Happened\n- Tab t3 was already on Content Wizard Step 4 Generate (from earlier session)\n- Clicked \"🚀 Generate Video\" → pipeline started → **CRASHED** at Step 8\n- Error: `NameError: name 'ratio' is not defined`\n\n### Root Cause — Pre-existing Bug in git HEAD\n- commit `6bde71d` (Printful API fix, \"06/17/2026\") removed the `ratio: str = \"9:16\"` parameter from `generate_video()` function signature\n- Also removed `\"ratio\": ratio` from Wan config payload\n- **BUT LEFT** `logger.info(f\"  Video OK ({file_size} bytes, {resolution} {ratio}): {result_path}\")` still referencing `ratio`\n- When pipeline reaches Step 8 → `NameError: name 'ratio' is not defined`\n\n### My Earlier Fixes Had Accidentally Fixed This Too\n- I removed the `{ratio}` reference from the log line as part of my broader edits\n- When user ordered `git checkout modules/video/pipeline_affiliate.py`, it reverted to HEAD (with this bug)\n- So the revert brought back a pre-existing bug that I had unknowingly fixed\n\n### Current Status\n- Pipeline stuck — cannot generate videos with current HEAD code\n- User's earlier instruction was to revert → now the reverted code has this P0 bug\n- Waiting for user to tell me how to proceed (fix the specific line or allow broader fixes)\n\n### Browser State\n- Tab t3: Content Wizard Step 4 Generate — shows error/processing status\n- Tab t9: TikTok Dashboard (unused due to BotStopper on openhands.m2igen.com)\n- BotStopper blocks automated browsing on `openhands.m2igen.com` — only tab t3 works because it navigated before BotStopper was fully blocking\n\n## Part 18 — 🐞 Etsy POD Wizard Fixes: image URL + step navigation bug (2026-07-13 16:38)\n\n### What Was Fixed\n1. **AI-generated image not showing** (`/storage/images/...` broken URL)\n   - Root cause: Prodia image-gen returns `/storage/images/nano_xxx.png` (relative local path)\n   - Browser on `m2igen.com/etsy/pod-wizard` tried to load `m2igen.com/storage/...` which didn't exist\n   - Fix: Backend rewrites to `/etsy/storage/...` in `ai_generate_image()` endpoint\n   - Nginx location `/etsy/storage/` → `http://127.0.0.1:8110/storage/` added\n   - Cache header `proxy_cache_valid 200 1h` + `expires 1h`\n\n2. **Step navigation broke after selecting variant**\n   - Root cause: `goNext()` stored `window.S = d` (full API response `{ok, session, step_result, ...}`) but initialization and all other functions used `window.S` as session object (`S.session_id`, `S.product_id`, `S.current_step_idx`)\n   - Result: `S.session_id` and `S.product_id` became `undefined` after first Next click\n   - `fillPrintInfo()` crashed with error toast because `S.product_id` was undefined\n   - Fix: `goNext()` stores `window.S = d.session || d` (consistent session object)\n   - `autoStartWizard()` stores `window.S = finalState.session`\n   - Turbo mode goNext: check `S.current_step_idx` directly (not `S.session.current_step_idx`)\n\n### Test Result\n- Poster flow: `provider → category → product → variant(White/18x24) → print_info(12000x9000px@300dpi) → artwork` ✅\n- Image URL: `https://m2igen.com/etsy/storage/images/nano_xxx.png` returns 200 OK\n\n### Key Files Modified\n| File | Change |\n|------|--------|\n| `/home/openhands/erp-stack/etsy-wizard/main.py` | Rewrite `/storage/` to `/etsy/storage/` in image URL |\n| `/home/openhands/erp-stack/etsy-wizard/static/pod-wizard.html` | `goNext()` stores `d.session \|\| d`; `autoStartWizard` uses `.session` |\n| `/etc/nginx/sites-enabled/m2igen.com.conf` | Added `/etsy/storage/` location block |\n"
}

## Part 19 — REAL food photos from internet (2026-07-13 16:42)

### What Changed
- **Deleted all AI-generated images** (KREA v2 Medium on fal.ai)
- **Downloaded 39 REAL food photos** from internet via `ddgs` (DuckDuckGo Image Search Python library)
- **Sources**: Freepik, Vecteezy, Dreamstime, SeriousEats, etc.
- **Total**: 39 images, 8.5 MB (vs 43 MB for AI)
- **Express server port changed** to 54531 (nginx config removed, Express serves directly)
- **Cache headers**: `no-store, no-cache, must-revalidate` + `?v=2` cache-busting
- **All HTTP 200 verified**

### Script Created
- `/home/openhands/super-appsheet/scripts/get_real_images.py` — DuckDuckGo image search downloader

### Key Facts
- **ddgs** (Python library) = DuckDuckGo image search — works without API key
- **Rate limit**: ddgs needs 2-3 second delay between requests
- **Image quality varies**: 23 KB (BEV001 Thai Iced Tea) to 1656 KB (APP006 Spring Rolls)

## Part 20 — TikTok Affiliate Lip-Sync Fix + Pipeline Run (2026-07-13 ~23:00)

### What Was Done
1. **Added `audio_path` parameter to `generate_video()`** — sends TTS WAV to Prodia Wan 2.7 for lip-sync driving
   - File: `/home/openhands/erp-stack/modules/video/pipeline_affiliate.py`
   - Multipart field: `("audio", ("audio.wav", audio_data, "audio/wav"))`
   - **No `audio` key needed in JSON config** — Prodia auto-detects the file

2. **`ratio` bug fixed** (pre-existing in git HEAD from commit 6bde71d)
   - `ratio` parameter removed from function signature but 3 references left in code
   - Fixed: removed `ratio` from 2 log lines + Wan config payload

3. **Atempo 1.3x speed up** — TTS ~15s → ~11.5s, video trimmed to 8s by `-shortest`

### Pipeline Run Result
- **Duration**: 129.2s (with audio sent to Wan 2.7)
- **Image**: `nano_b7e96ee1a632.png` (768×1344, 9:16)
- **Video**: `affiliate_6929b850_bgm.mp4` (with voice merge Step 9b + BGM mix)
- **Cost**: ~$0.074 per clip

### Wan 2.7 Lip-Sync Notes
- Audio input drives lip-sync conditioning ONLY — output audio is model-generated ambient noise (NOT TTS passthrough)
- Step 9b ALWAYS replaces Wan 2.7 output audio with clean TTS WAV
- Lip-sync timing comes from the video generation process (Wan 2.7 adjusts mouth shapes to match audio)

### Git Commits
- `ef7f585` — `fix: TikTok Studio pipeline fixes + POD wizard updates` (13 files)
- `80e643f` — `fix: TTS atempo 1.3x speed up` (1 file)
- Clean working tree at session end
